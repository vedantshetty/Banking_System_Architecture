@import url("https://fonts.googleapis.com/css?family=Mukta:300,400,700");
.tooltip,
.container__column:before,
.container__stack:before,
.container__single:before,
.heading
    position absolute
    right 0
    bottom 100%
    color #fff
    background #ffb238
    text-transform uppercase
    font-size 0.9rem
    padding 0.25rem 0.75rem
    border-radius 2.5px

.card,
.container__column div,
.container__stack div
    line-height 2
    background #fff
    padding 1.2rem 1rem
    border-radius 4px
    box-shadow 0 2px 10px #e6e6e6

*
    box-sizing border-box
    margin 0
    padding 0

body
    min-height 100vh
    width 100%
    font-family 'Mukta', sans-serif
    color #5f39dd
    background #5f39dd

.container
    margin 5vh 2.5vw
    padding 15vh 0
    background #fff
    border-radius 5px
    display flex
    flex-direction column
    align-items center
    text-align center
    svg
        height 5rem
        line
            stroke #5f39dd
            stroke-width 3px
            stroke-linecap round
            stroke-dasharray 2px 20px
            animation animateline 5s linear both infinite

h3
    font-size 1.1rem
    color #411fb2
    text-align center

p
    font-size 0.95rem
    font-weight 300
    text-align center

.title
    padding-bottom 5rem
    font-size 3rem

.container__column
    display flex
    border-radius 8px
    padding 1.5rem
    background #f9f9f9
    position relative
    div
        text-align left
        margin 0 1rem

.container__stack
    padding 5vh 5vw
    border-radius 8px
    background #f9f9f9
    position relative
    div
        margin 2rem 0
        svg
            width 4rem
            height auto
            fill #5f39dd

.container__single
    background #f9f9f9
    padding 1.5rem
    border-radius 8px
    position relative

@media (max-width: 700px)
    .container__sources
        flex-direction column
        div
            margin 1rem 0

@-moz-keyframes
    animateline
        from {
    stroke-dashoffset 0

to
    stroke-dashoffset -5rem
    stroke-dashoffset -5rem
    stroke-dashoffset -5rem
    stroke-dashoffset -5rem

}
    @-webkit-keyframes
        animateline
            from {
    stroke-dashoffset 0
    @-o-keyframes
        animateline
            from {
    stroke-dashoffset 0
    @keyframes
        animateline
            from {
    stroke-dashoffset 0
    .container__authentication
        h3
            letter-spacing 0.1rem

.container__API
    &:before
        content 'API Management'

.container__oauth
    &:before
        content 'OAuth'

.container__authentication
    &:before
        content 'Authentication'

.container__accessControl
    &:before
        content 'Access Control'

.container__presentation
    &:before
        content 'Presentation'

.container__moderatedResources
    &:before
        content 'Moderated Resources'

.container__OBPModels
    &:before
        content 'OBP Models'

.container__resourceAccess
    &:before
        content 'Resource Access'

.container__monitoring
    &:before
        content 'Monitoring'

.container__adapters
    &:before
        content 'Adapters'

.container__dataServices
    &:before
        content 'Data & Services'